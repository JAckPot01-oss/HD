# 一期系统设计说明

## 目标

把现有“Excel计算 + Word链接对象 + 人工复制修改”的流程改造成：

`施工信息 → 结构化参数 → 确定性计算/规则匹配 → 可审计结果 → BOM与计算书输出`

## 当前基线

- 平台材料：采用《中国人工智能（广州）产业园项目平台板等计算.xls》的首截面几何逻辑。
- 钢管扣件：采用《沈阳中心大厦结构件统计.xls》的材料分类，并改成边段/节点驱动。
- 连接构件：采用沈阳表中的“对面有板、梁内型钢、钢柱干涉、变截面”等条件规则。
- 机位验算：一期先实现 JFYM150B1-B2 常规机位；结果已与基准 Excel 回归核对。

## 核心实体

1. 项目：项目名称、建筑高度、层高、设计阶段。
2. 截面：截面编号、适用楼层、版本。
3. 爬模单元：筒号、方向、内外墙、架体型号。
4. 平台边段：方向、长度、平台层、护栏/翻板/封板配置。
5. 楼层机位条件：墙厚、楼板、型钢、钢柱、洞口和变截面条件。
6. 规则：条件表达式、优先级、构件输出、版本和审批状态。
7. 构件：编码、类别、规格、适用范围、单位和重量。
8. 结果：计算过程、规则命中记录、逐层明细、BOM和审计记录。

## 发现的旧流程风险

- JFYM150B1-B2 存在顶升2倍、系数修改、声屏障、悬挑等多个分支，不能用文件名或最后修改时间认定标准版本。
- 部分 Word 使用 `LINK Excel.Sheet.12` 指向原电脑 `E:\工作\计算书通用\...`，离开原目录后容易失联。
- Word 中大量计算结果以 OLE 对象嵌入，难以检索、对比和批量更新。
- 多个历史 Excel 把荷载系数、构件重量和几何常数直接写进公式，需要改成有版本的参数库。
- 当前连接件选型规则仍需企业设计负责人确认后才能从“待复核”升级为“批准”。

## 版本策略

- 基础模型：JFYM100B、JFYM150B1-B2、内墙小架体、内墙大架体。
- 工况插件：常规、悬挑、声屏障/广告字、顶升2倍、梁洞口、铝模。
- 规则状态：草稿、待复核、批准、停用。
- 每次成果记录输入快照、模型版本、规则版本和人工覆盖原因。

## 下一阶段

1. 由设计负责人确认荷载组合系数和螺栓强度设计值的当前企业标准。
2. 完成 JFYM100B、内墙大/小架体的全公式迁移与回归测试。
3. 把悬挑、声屏障和顶升2倍从历史副本提炼成明确插件。
4. 将现有 Word 计算书改造成无外部 Excel 链接的字段模板。
5. 接入项目级保存、版本对比、审签和正式 Excel/Word 成果。
